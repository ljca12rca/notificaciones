package org.example.notification.sms;

import org.example.notification.NotificationProvider;

import java.util.Map;

/**
 * Interfaz para proveedores de SMS (Twilio, etc.)
 */
public interface SmsProvider extends NotificationProvider {
    // Métodos específicos de SMS pueden ser añadidos aquí en el futuro
}